<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Badge d'Événement</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      color: #333;
      background-color: #fef9c3;
      font-size: 11px;
    }
    .container {
      max-width: 550px;
      margin: 0 auto;
      padding: 0;
      background-color: #fff;
      border-radius: 15px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.2);
      overflow: hidden;
      border: 1px solid #ddd;
      position: relative;
    }
    .badge-header {
      background-color: #f8dc3d;
      padding: 15px 20px;
      text-align: center;
      position: relative;
      border-bottom: 3px solid #e6c300;
    }
    .badge-title {
      font-size: 22px;
      font-weight: bold;
      margin-bottom: 4px;
      color: #333;
    }
    .badge-year {
      font-size: 26px;
      font-weight: bold;
    }
    .badge-content {
      padding: 20px;
      position: relative;
    }
    .badge-corner {
      position: absolute;
      width: 40px;
      height: 40px;
      border-radius: 50%;
      background-color: #f8dc3d;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: bold;
      color: #333;
      z-index: 2;
    }
    .top-left {
      top: -15px;
      left: -15px;
    }
    .top-right {
      top: -15px;
      right: -15px;
    }
    .bottom-left {
      bottom: -15px;
      left: -15px;
    }
    .bottom-right {
      bottom: -15px;
      right: -15px;
    }
    .profile-section {
      display: flex;
      flex-direction: column;
      align-items: center;
      margin-bottom: 20px;
      text-align: center;
      position: relative;
    }
    .profile-photo {
      width: 180px;
      height: 180px;
      border-radius: 50%;
      object-fit: cover;
      border: 5px solid #f8dc3d;
      margin-bottom: 15px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
    }
    .user-name {
      font-weight: bold;
      font-size: 24px;
      margin-bottom: 5px;
      color: #222;
    }
    .user-position {
      color: #444;
      font-size: 18px;
      margin-bottom: 5px;
      font-style: italic;
    }
    .user-details {
      font-size: 14px;
      margin-bottom: 3px;
      color: #555;
    }
    .badge-info {
      display: flex;
      margin: 15px 0;
      background-color: #f9f9f9;
      border-radius: 10px;
      padding: 12px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.05);
      border: 1px solid #eee;
    }
    .badge-info-item {
      flex: 1;
      text-align: center;
      padding: 0 5px;
    }
    .badge-info-title {
      font-size: 12px;
      font-weight: bold;
      color: #666;
      margin-bottom: 5px;
      text-transform: uppercase;
    }
    .badge-info-value {
      font-size: 16px;
      font-weight: bold;
      color: #333;
    }
    .badge-divider {
      height: 40px;
      width: 1px;
      background-color: #ddd;
      margin: 0 10px;
    }
    .qr-section {
      display: flex;
      flex-direction: column;
      align-items: center;
      margin: 15px 0;
    }
    .qr-code {
      width: 200px;
      height: 200px;
      padding: 10px;
      background-color: white;
      border: 3px solid #f0f0f0;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }
    .qr-code img {
      width: 100%;
      height: 100%;
    }
    .qr-caption {
      font-size: 12px;
      color: #666;
      margin-top: 8px;
      font-weight: bold;
    }
    .badge-footer {
      background-color: #f8dc3d;
      padding: 10px;
      text-align: center;
      font-size: 10px;
      color: #333;
      font-weight: bold;
    }
    .badge-id {
      position: absolute;
      top: 20px;
      right: 20px;
      background-color: #f8dc3d;
      padding: 5px 10px;
      border-radius: 20px;
      font-weight: bold;
      font-size: 12px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Badge Corners -->
    <div class="badge-corner top-left">RNP</div>
    <div class="badge-corner top-right"><%= new Date().getFullYear() %></div>
    <div class="badge-corner bottom-left">TN</div>
    <div class="badge-corner bottom-right">ID</div>
    
    <!-- Badge Header -->
    <div class="badge-header">
      <div class="badge-title">Première Réunion Nationale des Présidents</div>
      <div class="badge-year"><%= new Date().getFullYear() %></div>
    </div>
    
    <!-- Badge Content -->
    <div class="badge-content">
      <div class="badge-id">ID: RNP-<%= subscriptionFormId %></div>
      
      <!-- Profile Section -->
      <div class="profile-section">
        <% if (profilePictureDataUrl) { %>
          <img class="profile-photo" src="<%= profilePictureDataUrl %>" alt="Profile Photo">
        <% } else { %>
          <div class="profile-photo" style="background-color: #f0f0f0; display: flex; justify-content: center; align-items: center;">
            <span style="font-size: 80px; color: #888;">👤</span>
          </div>
        <% } %>
        
        <div class="user-name"><%= subscriptionForm.user.fullName %></div>
        <div class="user-position"><%= subscriptionForm.positionType %> <%= subscriptionForm.positionTitle ? `- ${subscriptionForm.positionTitle}` : '' %></div>
        <div class="user-details"><%= subscriptionForm.user.email %></div>
        <div class="user-details"><%= subscriptionForm.olm.name || subscriptionForm.olm.id %></div>
      </div>
      
      <!-- Badge Info -->
      <div class="badge-info">
        <div class="badge-info-item">
          <div class="badge-info-title">Date</div>
          <div class="badge-info-value"><%= new Date().toLocaleDateString('fr-FR', { day: 'numeric', month: 'long'}) %></div>
        </div>
        
        <div class="badge-divider"></div>
        
        <div class="badge-info-item">
          <div class="badge-info-title">Type</div>
          <div class="badge-info-value"><%= subscriptionForm.subscriptionOption.subscriptionType %></div>
        </div>
        
        <div class="badge-divider"></div>
        
        <div class="badge-info-item">
          <div class="badge-info-title">Montant</div>
          <div class="badge-info-value"><%= subscriptionForm.subscriptionOption.price %> TND</div>
        </div>
      </div>
      
      <!-- QR Code Section -->
      <div class="qr-section">
        <div class="qr-code">
          <img src="<%= qrCodeDataUrl %>" alt="QR Code">
        </div>
        <div class="qr-caption">Scannez pour vérifier l'abonnement</div>
      </div>
    </div>
    
    <!-- Badge Footer -->
    <div class="badge-footer">
      ID: <%= subscriptionForm.uuid %> | Valide jusqu'au 31 décembre <%= new Date().getFullYear() %>
    </div>
  </div>
</body>
</html>